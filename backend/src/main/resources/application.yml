spring:
  application:
    name: samofujera
  flyway:
    enabled: true
    locations: classpath:db/migration
  jooq:
    sql-dialect: postgres
server:
  port: 8080
management:
  endpoints:
    web:
      exposure:
        include: health,info
