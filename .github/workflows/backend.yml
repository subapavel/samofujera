name: backend

on:
  push:
    branches: [main, develop]
    paths: ['apps/backend-quarkus/**']
  pull_request:
    paths: ['apps/backend-quarkus/**']

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25
      - name: cache maven
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('apps/backend-quarkus/pom.xml') }}
      - name: test
        working-directory: apps/backend-quarkus
        run: ./mvnw verify

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25
      - name: build jar
        working-directory: apps/backend-quarkus
        run: ./mvnw package -DskipTests
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - name: deploy
        working-directory: apps/backend-quarkus
        run: flyctl deploy --local-only
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
